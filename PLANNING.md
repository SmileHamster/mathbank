# 수학 문제은행 + 시험지 자동 생성 시스템
## 포트폴리오 1호 프로젝트 — 4주 개발 계획

> 이 문서는 개발 시작 전 세운 원래 계획서입니다. 완성된 프로젝트 소개는 [README.md](README.md)를 참고하세요.

---

## 프로젝트 개요

**한 문장 정의**
학년·단원·유형·난이도 태그 기반 수학 문제은행에서 조건에 맞는 시험지를 자동 생성·출력하는 시스템.

**기술 스택**
- Backend: Java 17, Spring Boot 3.x, MyBatis, Spring Security
- DB: MariaDB (로컬), AWS RDS for MariaDB (운영)
- Frontend: Thymeleaf + Bootstrap (또는 Tailwind)
- 수식 렌더링: KaTeX (클라이언트 사이드)
- PDF 출력: OpenPDF + 한글 폰트 임베딩 (나눔고딕)
- 차트: Chart.js
- 배포: AWS EC2 + RDS, GitHub Actions

**구조 방침**
모놀리식으로 구현하되, 패키지는 MSA 분리 경계 기준으로 미리 분리
```
src/main/java/com/yourname/mathbank/
  ├── auth/        → 인증 (2호: Auth 서비스)
  ├── problem/     → 문제·태그 (2호: Problem 서비스)
  ├── examsheet/   → 시험지 생성 (2호: ExamSheet 서비스)
  ├── attempt/     → 응시·통계 (2호: Attempt 서비스)
  └── common/      → 공통 유틸
```

---

## ERD 핵심 엔티티 (7개)

```
Member          : id, username, password, role(ADMIN/STUDENT), created_at
Problem         : id, title, content(LaTeX 텍스트), answer, explanation, created_at
Tag             : id, tag_type(GRADE/CURRICULUM/TYPE/DIFFICULTY), tag_value, sort_order
ProblemTag      : problem_id, tag_id  ← 복합 PK, 복합 인덱스
ExamSheet       : id, name, grade_tag_id, created_by, created_at, total_count
ExamSheetProblem: exam_sheet_id, problem_id, sort_order  ← 복합 PK
Student         : id, name, grade_tag_id, memo, created_at
StudentAnswer   : id, student_id, exam_sheet_id, problem_id, is_correct, submitted_at
```

**태그 6축 분류 (수학교육과정 기반)**
- GRADE: 중1 / 중2 / 중3 / 고1 / 고2 / 고3
- SEMESTER: 1학기 / 2학기
- UNIT: 대단원명 (예: 일차함수, 확률과 통계)
- SUB_UNIT: 소단원명 (예: 일차함수의 그래프)
- TYPE: 개념확인 / 계산 / 서술 / 응용
- DIFFICULTY: 하 / 중 / 상

---

## 4주 개발 계획

> 기준: 평일 저녁 2~3시간 + 주말 6~8시간

### 큰 그림

| 주차 | 목표 | 마감 마일스톤 |
|------|------|--------------|
| Week 1 | 기반 + 문제 CRUD + 수식 렌더링 | 문제 등록·수정·삭제, 태그 부여, 수식이 화면에 보임 |
| Week 2 | 검색 + 시험지 자동 생성 | 조건 입력 → 시험지 화면 미리보기 |
| Week 3 | PDF 출력 + 학생 응시 + 통계 | PDF 다운로드, 학생 응시 후 오답 통계 |
| Week 4 | AWS 배포 + 마감 자료 | 외부 URL 시연, README, 시연 영상, 시드 데이터 100문제 |

> ⚠️ **핵심 원칙**: Week 1 마지막 날까지 "수식 포함 PDF 출력" PoC를 반드시 완료한다.
> 이 프로젝트의 가장 큰 기술 리스크가 여기에 집중되어 있다.

---

### Week 1 — 기반 다지기 + 기술 리스크 선제 검증

**Day 1~2 (평일)**
- [ ] Spring Boot 3.x + MyBatis + MariaDB 로컬 프로젝트 셋업
- [ ] Git 레포 생성, 패키지 구조 확정
- [ ] Thymeleaf + Bootstrap 도입, 공통 레이아웃 1개 작성

**Day 3 (평일)**
- [ ] ERD 확정 및 테이블 DDL 작성 (위 ERD 기준)
- [ ] 인덱스 설계: ProblemTag에 복합 인덱스 (problem_id, tag_id)

**Day 4 (평일)**
- [ ] Spring Security 최소 설정 (관리자 1명 로그인만)
- [ ] 공통 예외처리, 로깅, MyBatis 페이징 유틸 작성

**Day 5~7 (주말)**
- [ ] 문제 CRUD 화면 (목록·등록·수정·상세)
- [ ] 태그 다대다 관리 UI (6축 분류)
- [ ] 수식 입력: 본문은 LaTeX 텍스트로 DB 저장, 화면은 KaTeX 렌더링
- [ ] **[필수 PoC]** 수식 포함 문제 1개를 PDF로 출력하는 PoC 완성
  - OpenPDF 라이브러리 의존성 추가
  - KaTeX → PNG 변환 후 PDF 임베딩 방식으로 우선 검증
  - 한글 폰트(나눔고딕) 임베딩 테스트

> 🚨 **리스크 알람**: Day 7에 PDF PoC가 미완이면, Day 8(월요일)에 무조건 해결하거나
> 대체안(수식을 이미지로 미리 렌더링 후 DB BLOB 저장)으로 전환 결정. Week 2로 절대 미루지 않는다.

---

### Week 2 — 검색 + 시험지 자동 생성

**Day 8~10 (평일)**
- [ ] 다중 태그 + 키워드 문제 검색 구현
  - MyBatis 동적 쿼리 (`<foreach>`, `<if>`)로 조건 조합
  - 검색 결과 페이징, 정렬 (난이도순, 등록일순)
- [ ] 검색 화면 UI (태그를 칩 형태로 표시)

**Day 11 (평일)**
- [ ] 시험지 생성 도메인 모델 정리
  - 입력 조건 정의: 학년, 단원 목록, 난이도 분포(예: 하 30% / 중 50% / 상 20%), 총 문항 수

**Day 12~14 (주말)**
- [ ] 시험지 자동 생성 알고리즘 구현
  - 1단계: 조건에 맞는 문제 풀(pool) 추출
  - 2단계: 난이도 분포에 맞춰 가중 랜덤 선택
  - 3단계: 같은 유형이 연속되지 않도록 셔플 규칙 적용
- [ ] 생성된 시험지 미리보기 화면 (KaTeX 수식 정상 렌더링)
- [ ] 시험지 저장 (ExamSheet, ExamSheetProblem)
- [ ] 재생성 버튼 (같은 조건으로 다시 뽑기)

> ✅ **Week 2 마일스톤**: 관리자가 조건 입력 → 시험지 화면 미리보기까지 동작

---

### Week 3 — PDF 출력 + 학생 응시 + 통계

**Day 15~17 (평일)**
- [ ] 시험지 PDF 출력 본격 구현 (Week 1 PoC → 실전 품질)
  - 문제지 / 답안지 분리 출력
  - 머리말(학년, 시험명, 날짜, 이름란), 문항 번호, 페이지 번호
  - 답안지: 정답 + 간단 해설 포함
  - 한글 폰트(나눔고딕) 임베딩 확인

**Day 18 (평일)**
- [ ] 학생 도메인 CRUD + 응시 기록 입력 화면
  - 학생이 시험지 선택 → 문항별 정답 체크 입력 (관리자 일괄 입력 형태로 단순화)

**Day 19~21 (주말)**
- [ ] 통계 화면 구현 (Chart.js)
  - 학생별: 시험지별 정답률, 단원별 정답률, 자주 틀린 유형 Top 3
  - 문제별: 전체 학생 대비 오답률

> ✅ **Week 3 마일스톤**: PDF 다운로드 + 학생 응시 입력 + 통계 화면까지 일관되게 동작

---

### Week 4 — AWS 배포 + 마감 자료 + 보강

**Day 22~23 (평일)**
- [ ] AWS 셋업
  - EC2 (t3.micro 또는 t3.small), 보안 그룹, 키 페어
  - RDS for MariaDB (프리티어 t3.micro), 파라미터 그룹
  - EC2 퍼블릭 IP + 80 포트로 운영 (도메인 없이 시작, 시간 절약)
- [ ] 배포 자동화 최소 버전
  - GitHub Actions → Gradle 빌드 → scp로 jar 전송 → systemd 재시작
- [ ] 환경 분리: `application-prod.yml`, RDS 접속 정보는 EC2 환경변수로

> 팁: Day 21(일요일 저녁)에 EC2 인스턴스만 미리 생성해두면 Day 22 진행이 빨라진다.

**Day 24 (평일)**
- [ ] 시드 데이터 100문제 입력 (한 학년 한 학기에 집중)
  - 권장: 단원 5~6개 × 15~20문제
  - 저작권: 직접 작성 또는 변형, 출처는 본인 작성으로 통일
  - 데모 시나리오: "중2 일차함수 단원 중상난이도 20문제 자동 생성"이 가장 임팩트 있음

**Day 25 (평일)**
- [ ] 시연 영상 녹화 (3~5분)
  - 시나리오: 문제 등록 → 검색 → 시험지 생성 → PDF 다운로드 → 학생 응시 입력 → 통계 확인
- [ ] README 작성
  - 프로젝트 소개 (스토리텔링 포함, 면접에서 그대로 인용 가능한 톤)
  - 기술 스택, ERD 이미지, 화면 캡처 5~6장
  - 실행 방법, 배포 URL
  - 의사결정 기록 (아래 항목 참고)

**Day 26~28 (주말 — 버퍼)**
- [ ] 밀린 작업 수습, 버그 수정, 코드 리팩토링
- [ ] 시간이 남으면 아래 우선순위로 보강

| 우선순위 | 보강 항목 |
|---------|----------|
| 1순위 | 핵심 서비스 단위 테스트 추가 (면접 어필) |
| 2순위 | Swagger / Spring REST Docs API 문서화 |
| 3순위 | 학생별 오답노트 PDF 출력 |
| 4순위 | MyBatis 2차 캐시 (Redis는 비추) |

---

## 위험구간 식별 및 대응책

| 위험 | 발생 시점 | 대응책 |
|------|----------|--------|
| 수식 + PDF 출력 기술 검증 실패 | Week 1 마지막 | KaTeX → PNG 렌더링 후 DB BLOB 저장, PDF에는 이미지로 임베딩 |
| 시드 데이터 입력 시간 초과 | Week 4 Day 24 | 범위 축소: 단원 5개 × 15문제 = 75문제로 타협 |
| AWS 배포 미숙으로 지연 | Week 4 Day 22~23 | Day 21 저녁에 EC2 인스턴스 미리 생성 (학습 비용 분산) |
| 평일 야근으로 시간 부족 | 상시 | Week 4 주말을 버퍼로 비워둔 이유. 평일 2일 빠져도 회수 가능 |

---

## 면접 의사결정 기록 (README에 포함)

> 아래 항목을 한두 줄씩 미리 적어두면 면접 답변이 자동으로 나온다.

| 결정 | 이유 |
|------|------|
| Thymeleaf 선택 | JSP 대비 미래지향적, React 대비 4주 마감에 적합 |
| OpenPDF 선택 | iText 라이선스 이슈(AGPL) 회피, 한글 폰트 임베딩 지원 |
| KaTeX 선택 | MathJax 대비 가볍고 빠름, 서버 사이드 렌더링 불필요 |
| 태그 6축 설계 | 수학교육과정 분류 체계(교육과정 문서) 직접 인용, 현장 강사 경험 기반 |
| 모놀리식 선택 | 4주 완성 우선, 도메인 경계는 패키지 수준으로 미리 분리 |
| MSA 전환 계획 | 2호 프로젝트에서 problem / examsheet / attempt / auth 4개 서비스로 분리 예정 |
| PDF 수식 렌더링 — 텍스트 치환 선택 | KaTeX는 JS 라이브러리로 JVM에서 직접 실행 불가. 브라우저-서버 런타임 경계 문제. `latexToText()` 정규식 치환(√, ², ×, ≤ 등 유니코드 변환)으로 타협. 대안인 headless Chrome 서버 방식은 구현 비용 과다로 포트폴리오 범위 초과 판단 |
| HWP 대신 PDF 선택 | Java용 HWP 라이브러리(poi-ooxml 등)는 HWP 형식 지원 미성숙, 파싱/생성 모두 불안정. SI 현장에서도 PDF가 범용적이며, OpenPDF 기반 서버 사이드 PDF 생성은 백엔드 기술로 더 명확히 어필 가능 |
| 페이지 번호 N/M — 2-pass 방식 | `PdfPageEventHelper.onEndPage()`에서 현재 페이지 기록 + `PdfTemplate` 플레이스홀더 삽입, `onCloseDocument()`에서 전체 페이지 수를 역으로 채움. 단순 순방향 출력으로는 총 페이지를 미리 알 수 없어 2-pass가 필수 |
| 한글 폰트 임베딩 방식 | `ClassPathResource`로 NanumGothic.ttf를 byte[]로 로드 후 `BaseFont.IDENTITY_H + EMBEDDED`로 등록. jar 내 완전 자급 구조로 서버 OS 폰트 환경에 의존하지 않음 |

---

## 면접 스토리텔링 초안

> README 소개 문단 또는 면접 답변으로 그대로 활용 가능

강사 시절 매주 시험지를 만들었다.
"중2 1학기 일차함수만 모아서 중상 난이도로 20문제" 같은 요구가 반복되는데
매번 hwp 파일에서 문제를 직접 골라 복붙하고 있었다.

그때 자동화하면 되겠다고 생각만 했던 것을 지금 직접 만들었다.

핵심은 태그 체계다.
학년·과정·대단원·소단원·유형·난이도라는 6축 분류는
수학교육과정을 직접 이수하고 현장에서 가르쳐본 사람이 아니면
정확하게 설계하기 어려운 구조다.
실무의 코드 분류 체계와 구조는 같지만, 도메인 깊이가 다른 부분이다.

이후 같은 도메인을 MSA + eGov 5.0으로 재구성하는 2호 프로젝트를 계획하고 있으며,
그것을 염두에 두고 1호에서도 패키지를 서비스 경계 기준으로 미리 분리해 두었다.

---

## PDF 출력 기술 의사결정 히스토리

### KaTeX와 OpenPDF의 런타임 경계 문제

브라우저에서 수식이 KaTeX로 잘 렌더링되기 때문에, 처음에는 PDF에도 같은 방식을 쓸 수 있다고 가정하기 쉽다.
그러나 KaTeX는 **JS 라이브러리**로 브라우저(또는 Node.js) 런타임에서만 동작한다.
OpenPDF는 **JVM 위에서 실행되는 Java 라이브러리**다. 두 런타임은 완전히 분리되어 있다.

따라서 Java 서버 코드에서 KaTeX를 직접 호출하는 것은 불가능하다.

**검토한 대안 3가지**

| 방식 | 설명 | 판단 |
|------|------|------|
| 텍스트 치환 (채택) | `latexToText()` 정규식으로 LaTeX → 유니코드 기호 변환 (`√`, `²`, `×`, `≤` 등) | 수식 품질 제한적이나 포트폴리오 범위 내 합리적 타협 |
| Headless Chrome | 서버에서 Chromium을 실행해 KaTeX를 렌더링한 HTML을 PDF로 변환 | 서버 환경 구성 복잡, 운영 비용 증가, 4주 범위 초과 |
| OZ Report 등 리포팅 툴 | 자체 렌더링 엔진 탑재, DB 연동 리포트 생성 가능 | JS 라이브러리 불가는 동일 (서버 사이드 렌더러라서). SI 현장 외부 라이선스 비용 |

**면접 어필 포인트**: 구현 한계를 인지하고 대안을 비교 분석한 뒤 범위에 맞는 선택을 설명할 수 있다는 점 자체가 경험으로 어필 가능하다. "완벽한 구현"보다 "트레이드오프를 이해한 의사결정"이 면접에서 더 유의미하다.

### HWP vs PDF

현장 강사 시절 사용한 형식은 hwp(한글)다. 그러나 Java에서 HWP를 프로그래밍으로 생성하는 것은 현실적으로 어렵다.
- HWP 형식 규격이 비공개 (일부 공개되어 있으나 불완전)
- Java용 HWP 라이브러리가 없거나 불안정
- poi-ooxml 계열에 HWP 지원 없음

PDF는 SI 프로젝트에서도 공문서·계약서 출력에 실제로 사용하는 형식이며,
서버 사이드 PDF 생성(OpenPDF, iText 계열) 경험은 백엔드 기술로 명확히 어필 가능하다.

---

## Claude Code 작업 지시 순서 (참고)

> 이 계획을 Claude Code에 넘길 때 추천하는 순서

1. `[Week1-1]` 프로젝트 셋업 + 패키지 구조 생성 + DDL 작성
2. `[Week1-2]` Spring Security 설정 + 공통 유틸 (페이징, 예외처리)
3. `[Week1-3]` Problem CRUD + Tag 다대다 + Thymeleaf 화면
4. `[Week1-4]` KaTeX 렌더링 확인 + OpenPDF PoC (수식 포함 PDF 출력)
5. `[Week2-1]` 다중 태그 검색 (MyBatis 동적 쿼리)
6. `[Week2-2]` 시험지 자동 생성 알고리즘 + 미리보기 화면
7. `[Week3-1]` PDF 출력 실전 품질 완성 (문제지/답안지 분리)
8. `[Week3-2]` 학생 응시 입력 + 통계 화면 (Chart.js)
9. `[Week4-1]` AWS EC2 + RDS 배포 + GitHub Actions
10. `[Week4-2]` 시드 데이터 입력 + README + 시연 영상
